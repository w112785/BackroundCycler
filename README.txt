To compile the souce code you must first download the google GDATA API .NET liberary and add references to "Google.GData.Photos.dll", "Google.GData.Client.dll", and "Google.GData.Extensions.dll" DLL's

the API is at this URL:         http://code.google.com/p/google-gdata/downloads/list